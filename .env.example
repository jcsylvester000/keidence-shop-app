# Neon.tech Postgres connection string (used when the DB is wired up).
# Get this from the Neon dashboard → Connection Details → Prisma.
DATABASE_URL="postgresql://USER:PASSWORD@ep-xxxx.us-east-2.aws.neon.tech/keidence?sslmode=require"
